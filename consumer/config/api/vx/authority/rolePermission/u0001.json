{
  "validation": {
    "rules": {
      "rolePermissionId": {
        "required": true
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "rolePermissionId": {
        "required": "角色与许可标识必填"
      },
      "name": {
        "required": "角色与许可名称必填",
        "minlength": "角色与许可名称需{0}个字符以上",
        "maxlength": "角色与许可名称需小于{0}个字符"
      }
    }
  }
}