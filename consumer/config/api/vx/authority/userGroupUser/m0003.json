{
  "validation": {
    "rules": {
      "userGroupUserCode": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "userGroupUserCode": {
        "required": "用户组与用户编号必填",
        "minlength": "用户组与用户编号需{0}个字符以上",
        "maxlength": "用户组与用户编号需小于{0}个字符"
      },
      "name": {
        "required": "用户组与用户名称必填",
        "minlength": "用户组与用户名称需{0}个字符以上",
        "maxlength": "用户组与用户名称需小于{0}个字符"
      }
    }
  }
}