{
  "validation": {
    "rules": {
      "roleCode": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "roleCode": {
        "required": "角色编号必填",
        "minlength": "角色编号需{0}个字符以上",
        "maxlength": "角色编号需小于{0}个字符"
      },
      "name": {
        "required": "角色名称必填",
        "minlength": "角色名称需{0}个字符以上",
        "maxlength": "角色名称需小于{0}个字符"
      }
    }
  }
}