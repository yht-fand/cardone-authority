{
  "validation": {
    "rules": {
      "userGroupRoleId": {
        "required": true
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "userGroupRoleId": {
        "required": "用户组与角色标识必填"
      },
      "name": {
        "required": "用户组与角色名称必填",
        "minlength": "用户组与角色名称需{0}个字符以上",
        "maxlength": "用户组与角色名称需小于{0}个字符"
      }
    }
  }
}