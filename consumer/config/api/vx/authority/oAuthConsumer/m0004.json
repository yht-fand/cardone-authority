{
  "validation": {
    "rules": {
      "oAuthConsumerCode": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "oAuthConsumerCode": {
        "required": "授权消费编号必填",
        "minlength": "授权消费编号需{0}个字符以上",
        "maxlength": "授权消费编号需小于{0}个字符"
      },
      "name": {
        "required": "授权消费名称必填",
        "minlength": "授权消费名称需{0}个字符以上",
        "maxlength": "授权消费名称需小于{0}个字符"
      }
    }
  }
}