{
  "validation": {
    "rules": {
      "oauthConsumerId": {
        "required": true
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "oauthConsumerId": {
        "required": "授权消费标识必填"
      },
      "name": {
        "required": "授权消费名称必填",
        "minlength": "授权消费名称需{0}个字符以上",
        "maxlength": "授权消费名称需小于{0}个字符"
      }
    }
  }
}