{
  "validation": {
    "rules": {
      "userGroupId": {
        "required": true
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "userGroupId": {
        "required": "用户组标识必填"
      },
      "name": {
        "required": "用户组名称必填",
        "minlength": "用户组名称需{0}个字符以上",
        "maxlength": "用户组名称需小于{0}个字符"
      }
    }
  }
}